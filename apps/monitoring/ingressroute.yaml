---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: grafana-ingressroute
  namespace: monitoring
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`grafana.tomerc.com`)
      kind: Rule
      services:
        - name: kube-prometheus-stack-grafana
          port: 80
