apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: argocd-ingressroute
  namespace: argocd
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`{{ argocd_ingress_url }}`)
      kind: Rule
      services:
        - name: argocd-server
          port: 80
